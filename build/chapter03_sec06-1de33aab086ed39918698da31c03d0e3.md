# Dimensionsformel

Kern und Bild einer Matrix beschreiben zwei grundlegende Aspekte einer linearen
Abbildung. Bisher haben wir sie getrennt berechnet. In diesem Kapitel lernen wir
einen eleganten Zusammenhang zwischen der Dimension des Kerns und der Dimension des
Bildes kennen: die Dimensionsformel. Sie ermöglicht es, die Dimension des einen aus
der Dimension des anderen zu bestimmen, und liefert ein wichtiges Kriterium für
die Lösbarkeit linearer Gleichungssysteme.

## Lernziele

```{admonition} Lernziele
:class: attention
* Sie kennen die **Dimensionsformel** und können sie anwenden:
  \begin{equation*}
  \dim(\text{Kern}(\mathbf{A})) + \dim(\text{Bild}(\mathbf{A})) = n,
  \end{equation*}
  wobei $n$ die Anzahl der Spalten der Matrix $\mathbf{A} \in \mathbb{R}^{m \times n}$ ist.
* Sie können aus der Dimension des Kerns die Dimension des Bildes bestimmen und
  umgekehrt.
* Sie können die Lösbarkeit eines linearen Gleichungssystems $\mathbf{A} \cdot \vec{x}
  = \vec{b}$ mit dem Kriterium $\vec{b} \in \text{Bild}(\mathbf{A})$ beurteilen.
```

## Die Dimensionsformel

Die Dimensionsformel stellt einen fundamentalen Zusammenhang zwischen dem Kern und
dem Bild einer linearen Abbildung her. Für eine Matrix $\mathbf{A} \in \mathbb{R}^{m \times n}$,
die also $n$ Spalten hat, gilt:

```{admonition} Was ist ... die Dimensionsformel?
:class: note
Sei $\mathbf{A} \in \mathbb{R}^{m \times n}$ eine Matrix. Dann gilt:

\begin{equation*}
\dim(\text{Kern}(\mathbf{A})) + \underbrace{\dim(\text{Bild}(\mathbf{A}))}_{\text{Rang}(\mathbf{A})} = n.
\end{equation*}

Die Summe aus der Dimension des Kerns und dem Rang der Matrix ergibt immer die
Anzahl der Spalten der Matrix.
```

Die Formel sagt anschaulich folgendes: Von den $n$ Dimensionen des Eingaberaums
$\mathbb{R}^n$ werden $\dim(\text{Kern}(\mathbf{A}))$ Dimensionen durch die Abbildung
auf null reduziert (sie gehen im Kern verloren). Die verbleibenden
$\dim(\text{Bild}(\mathbf{A})) = \text{Rang}(\mathbf{A})$ Dimensionen werden
tatsächlich auf den Ausgaberaum abgebildet und bilden das Bild.

## Anwendung der Dimensionsformel

Die Dimensionsformel erlaubt es, aus einer bekannten Größe direkt die andere zu
berechnen, ohne erneut ein Gleichungssystem lösen zu müssen.

**Beispiel 1:** Wir betrachten erneut die Matrix

\begin{equation*}
\mathbf{D} = \begin{pmatrix} 1 & 2 & 2 \\ 1 & -1 & 2 \\ 0 & 0 & 0 \\ 0 & 1 & 0 \end{pmatrix}
\in \mathbb{R}^{4 \times 3}.
\end{equation*}

Im vorigen Kapitel haben wir berechnet:

\begin{equation*}
\text{Kern}(\mathbf{D}) = \left\{ t \cdot \begin{pmatrix} -2 \\ 0 \\ 1 \end{pmatrix}
\;\middle|\; t \in \mathbb{R} \right\}, \quad
\dim(\text{Kern}(\mathbf{D})) = 1.
\end{equation*}

Die Matrix hat $n = 3$ Spalten. Die Dimensionsformel liefert sofort:

\begin{equation*}
\dim(\text{Bild}(\mathbf{D})) = n - \dim(\text{Kern}(\mathbf{D})) = 3 - 1 = 2.
\end{equation*}

Das Bild wird also von zwei linear unabhängigen Vektoren aufgespannt. Dieses Ergebnis
stimmt mit der direkten Berechnung im vorigen Kapitel überein.

**Beispiel 2:** Für eine $3 \times 5$-Matrix $\mathbf{A}$, also $n = 5$ Spalten, sei
bekannt, dass $\text{Rang}(\mathbf{A}) = 3$ ist. Die Dimensionsformel liefert:

\begin{equation*}
\dim(\text{Kern}(\mathbf{A})) = 5 - 3 = 2.
\end{equation*}

Der Kern hat also die Dimension 2, das bedeutet es gibt zwei linear unabhängige
Richtungen im $\mathbb{R}^5$, die durch die Abbildung auf den Nullvektor fallen.

**Beispiel 3:** Eine invertierbare $n \times n$-Matrix hat $\text{Rang}(\mathbf{A}) = n$,
da alle $n$ Spalten linear unabhängig sind. Die Dimensionsformel ergibt:

\begin{equation*}
\dim(\text{Kern}(\mathbf{A})) = n - n = 0.
\end{equation*}

Der Kern enthält nur den Nullvektor. Das bedeutet, die Abbildung ist umkehrbar und
die inverse Matrix $\mathbf{A}^{-1}$ existiert. Diese Aussage stimmt mit dem früheren
Ergebnis überein, dass eine Matrix genau dann invertierbar ist, wenn ihre Determinante
von null verschieden ist.

## Lösbarkeit linearer Gleichungssysteme

Die Dimensionsformel hat eine unmittelbare Anwendung in der Frage nach der Lösbarkeit
linearer Gleichungssysteme. Wir fragen: Für welche Vektoren $\vec{b}$ ist das
Gleichungssystem

\begin{equation*}
\mathbf{A} \cdot \vec{x} = \vec{b}
\end{equation*}

lösbar? Die Antwort folgt direkt aus der Definition des Bildes:

\begin{equation*}
\mathbf{A} \cdot \vec{x} = \vec{b} \text{ ist lösbar} \quad \Longleftrightarrow \quad
\vec{b} \in \text{Bild}(\mathbf{A}).
\end{equation*}

Das Gleichungssystem ist genau dann lösbar, wenn der Vektor $\vec{b}$ im Bild der
Matrix liegt, also als Linearkombination der Spalten von $\mathbf{A}$ darstellbar ist.

**Beispiel:** Für die Matrix
$\mathbf{A} = \begin{pmatrix} 1 & 0 \\ 0 & 0 \end{pmatrix}$
hat das Bild die Dimension 1 und wird von $\begin{pmatrix} 1 \\ 0 \end{pmatrix}$
aufgespannt. Das Gleichungssystem $\mathbf{A} \cdot \vec{x} = \vec{b}$ ist also
lösbar für alle Vektoren der Form $\vec{b} = \begin{pmatrix} c \\ 0 \end{pmatrix}$
mit $c \in \mathbb{R}$. Für $\vec{b} = \begin{pmatrix} 2 \\ 3 \end{pmatrix}$
dagegen ist das System unlösbar, weil $\begin{pmatrix} 2 \\ 3 \end{pmatrix}$ nicht
im Bild liegt.

## Übersicht: Kern, Bild, Rang und Dimensionsformel

Die folgende Tabelle fasst die wichtigsten Zusammenhänge zusammen:

| Situation | Kern | Bild | Rang |
| --- | --- | --- | --- |
| Invertierbare $n\times n$-Matrix | nur $\vec{0}$ | gesamter $\mathbb{R}^n$ | $n$ |
| Singulare $n\times n$-Matrix | mehr als $\vec{0}$ | echter Teilraum | $< n$ |
| Projektion auf Unterraum | mindestens 1D | Unterraum | $< n$ |

```{dropdown} Video "Dimensionsformel (Kern und Bild)" von MathePeter
<iframe width="560" height="315" src="https://www.youtube.com/embed/C-Ek0V8MxCo"
title="YouTube video player" frameborder="0" allow="accelerometer; autoplay;
clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
```

## Zusammenfassung und Ausblick

Die Dimensionsformel $\dim(\text{Kern}(\mathbf{A})) + \text{Rang}(\mathbf{A}) = n$
verbindet Kern und Bild einer linearen Abbildung zu einem vollständigen Bild: Von den
$n$ Eingabedimensionen werden einige im Kern "vernichtet" und die übrigen bilden das
Bild. Das Lösbarkeitsskriterium $\vec{b} \in \text{Bild}(\mathbf{A})$ verbindet diese
Theorie mit der praktischen Frage, wann ein lineares Gleichungssystem eine Lösung
besitzt. Im letzten Kapitel dieses Abschnitts lernen wir den Basiswechsel kennen, der
die Darstellung von Vektoren in verschiedenen Koordinatensystemen ermöglicht.
